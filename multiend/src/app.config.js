// Mini-program app manifest. `pages[0]` is the entry page. Add pages here as the
// migration grows (each page = a directory under src/pages with index + config).
export default {
  pages: ['pages/index/index'],
  window: {
    backgroundTextStyle: 'light',
    navigationBarBackgroundColor: '#ff8fa3',
    navigationBarTitleText: '小希 AI',
    navigationBarTextStyle: 'white',
  },
};
