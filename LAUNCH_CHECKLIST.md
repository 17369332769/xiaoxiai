# 小希 AI · 上线检查清单（LAUNCH CHECKLIST）

> 用途：从"可演示 Demo"走向"正式上线（含微信/支付宝小程序）"的可勾选清单。
> 来源：2026-06-30 对《需求分析文档.md》第 6 节状态总表的代码对账复核。
> 图例：☐ 待办 ｜ ✅ 已具备 ｜ 责任方：研发 / 运维 / 产品 / 法务 / 外部服务商。
> 说明：平台政策与备案口径随时间变化，最终以平台当时要求与法务意见为准。

## A. 法律/合规（P0，决定能否上线）

- ☐ **小程序类目可行性评估**（产品+法务）：微信/支付宝/抖音对「AI 情感陪伴/虚拟女友」审核极严，可能直接拒类目。建议把"女友"重定位为"AI 情感陪伴/倾诉"，去除强亲密表述后再报类目。**先做这一步，再决定后续投入。**
- ☐ **主体与资质**（产品+法务）：AI 类目个人主体多不可选 → 企业主体；按平台要求备齐类目资质/承诺函。
- ☐ **生成式 AI 算法备案**（法务）：面向公众的生成式 AI 服务需做算法备案并通过内容安全评估（《生成式人工智能服务管理暂行办法》）。周期长，尽早启动。
- ✅ **AI 生成内容显式标识**（研发，本轮已补）：聊天界面每条 AI 回复带可见"AI 生成"角标（《人工智能生成合成内容标识办法》2025-09-01 施行）。`sender='ai'` 作为持久化机器可读标记。
  - ☐ 补充：导出数据/TTS 音频的隐式（元数据）标识（研发，按平台要求）。
- ☐ **实名认证 + 青少年模式**（法务+外部服务商+研发）：当前 `users` 表无实名/年龄/青少年字段；未成年保护仅靠几个敏感词。需接入实名 API、年龄确认、青少年模式或明确"仅限成年人"准入。
- ☐ **隐私政策 / 服务条款法务定稿**（法务）：`public/privacy.html`、`public/terms.html` 仍为示例模板，需填真实主体、ICP/算法备案号、第三方清单、争议管辖；小程序后台「用户隐私保护指引」逐项如实声明。

## B. 基础设施（P0）

- ☐ **HTTPS + ICP 备案域名**（运维）：小程序强制。
- ☐ **小程序 appid**（运维）：`multiend/project.config.json` 的 `touristappid` 换成真实 AppID。
- ☐ **request 合法域名白名单**（运维）：开发者后台加入 API 域名 + TTS 音频域名（确认 MiniMax 返回的音频 URL 在白名单内或可公网访问）。
- ☐ **生产 `.env` 补齐密钥**（运维）：当前实际 `backend/.env` 仅 ~22 行，缺 `AUTH_SECRET`/`PAYMENT_SECRET`/`ADMIN_TOKEN`/`RATE_LIMIT_*`/`TRUST_PROXY` 等——否则在用硬编码 dev 默认密钥，是上线安全隐患。对照 `backend/.env.example` 与 `backend/OPERATIONS.md` 全量覆盖。
- ☐ **`TRUST_PROXY` 按反代拓扑设置**（运维）：设错会让限流/防爆破退化为全站单桶。

## C. 内容安全与风控（P1）

- ✅ **安全事件审计 + 可插拔三方审核骨架**（研发，本轮已补）：`content_safety_events` 表 + `moderateText`/`setModerationProvider`（词表为权威基线，provider 仅升级、出错回退）+ 运营后台"内容安全拦截记录"面板。
- ☐ **接入真实内容审核服务**（外部服务商+研发）：通过 `setModerationProvider()` 接阿里云/腾讯云内容安全（文本/图片），词表保留兜底。微信 UGC 类通常要求接入平台 `security.msgSecCheck`。
- ✅ **高频/异常行为风控**（研发，已补）：基于 `content_safety_events` 近窗口拦截频次的 `assessAbuse` 升级（flagged repeat-offender + 更强提示），`classifyRiskTopic` 风险话题分类骨架。临时禁言/账号级限制仍待产品定义。

## D. 业务闭环（P1）

- ☐ **真实支付商户号**（运维+研发）：申请微信/支付宝商户号，配置真实 `PAYMENT_SECRET` + 回调 URL（回调验签/幂等结算逻辑已就绪）。
- 🟡 **生产支付补单/对账**（研发，部分已补）：运营后台已加**手动补单** `/api/admin/order/settle`（复用幂等结算）+ **对账报表** `/api/admin/orders/reconcile`。仍待：用户端"重新查询并自助补单"入口（依赖真实网关回调）。
- ☐ **OTP 真实短信/邮件**（外部服务商+研发）：替换 `verification.js` 日志桩。
- ☐ **第三方/微信一键登录**（研发，可选）。
- 🟡 **运营后台 CRUD**（研发，大部分已补）：✅ **异常日志查看** `/api/admin/logs` + 面板；✅ **商品/任务增删改**——`shared/gameConfig.js` 已改为 DB 动态目录（`food_catalog`/`gift_catalog`/`task_catalog`，首启 seed），后台「目录管理」可增删改，前端经 `/api/user/sync` 的 `catalog` 字段拉取渲染。仍待：活动管理；删除任务后**老用户 `tasks` 表的历史行不自动清除**、新增**每日**任务的自动重置仍按静态 `DAILY_TASK_IDS`（动态每日任务暂不自动重置）——两处后续打磨。
- ✅ **背包资产明细 / 账单筛选**（研发，已补）：钱包页加资产概览（拥有主题数 / 近期获得 / 近期支出）+ 全部/收入/支出筛选。

## E. 质量与发布门禁（P0）

- ✅ **统一错误封套 + 限流 + CORS + 输入过滤**（研发，已具备）。
- ✅ **核心流程 + 支付/喂食幂等测试**（研发，已具备）。
- ✅ **断网自动重连 / 429 倒计时 / 重试体验**（研发，已补）：网络恢复 offline→online 自动 `retrySync`；429 本地化"请在 N 秒后再试"；签到/领奖的 `ALREADY_CHECKED_IN`/`TASK_NOT_CLAIMABLE` 重试已优雅处理（提示而非报错，后端原子守卫防重复发币）。
- ✅ **次日留存口径修正**（研发，本轮已补）。
- ☐ **每次发布前跑 `npm run verify` 全绿**（研发）：check:secrets → lint → 前端 vitest → 后端 node:test → build。
- ☐ **数据库备份与迁移演练**（运维）。

---

### 优先级速览

1. **A（合规）+ B（基础设施）** 是上线硬门槛，且 A 的"类目可行性 + 主体资质"决定要不要继续投入——**最先确认**。
2. **C/D** 是产品完整性与变现闭环。
3. **E** 贯穿每次发布。
